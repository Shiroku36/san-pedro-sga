@model IEnumerable<ControlPersonalAppWeb.Models.TrabajadorIndex>

@{
    ViewBag.Title = "RR.HH.";
    var cuenta = ControlPersonalAppWeb.Utils.SessionManager.CuentaAutenticada();
}
@if (!String.IsNullOrEmpty(ControlPersonalAppWeb.Utils.SessionManager.mensaje))
{
    <div class="alert-info">
        <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
        @ControlPersonalAppWeb.Utils.SessionManager.mensaje
    </div>
    ControlPersonalAppWeb.Utils.SessionManager.mensaje = "";
}
<h1 class="display-4 text-center">Índice RR.HH.</h1>
<hr />
<div class="form-group">
    @if (cuenta.Permisos.Contains("TrabajadoresC"))
    {
        <a href='@Url.Action("Create", "Trabajador")'>
            <label type="button" class="btn btn-primary float-right ml-2  mb-3">Crear Nuevo</label>
        </a>
    }
    <a href='@Url.Action("Hoy", "Registro")'>
        <label type="button" class="btn btn-primary float-right  ">Registros del día</label>
    </a>
    <!--
    <a href='@Url.Action("Borrar", "Trabajador")'>
        <button type="button" class="btn btn-default">borrar</button>
    </a>-->
</div>

<div class="clearfix"></div>
<div class="row">
    <div class="col-sm-2">
        <h4 class="busqueda">Búsqueda: </h4>
    </div>
    <div class="col-sm-8 form-label-group">
        <input type="text" id="busqueda" onkeyup="buscar()" class="mb-3 mt-1 form-control">
    </div>
</div>

<div class="table-responsive">
    <table class="table table-hover" id="tabla">
        <tr class="bg-primary">
            <th style="width:12%">
                @Html.DisplayNameFor(model => model.Rut)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Uid)
            </th>
            <th>
                Numero pulsera
            </th>
            <th style="width:40%">
                Accion
            </th>
        </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Rut
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nombre) @Html.DisplayFor(modelItem => item.ApellidoPaterno) @Html.DisplayFor(modelItem => item.ApellidoMaterno)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Uid)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Gerente)
                    </td>

                <td>
                    @if (cuenta.Permisos.Contains("RegistrosV"))
                    {
                        <a href='@Url.Action("Index", "Registro", new { id = item.Id })'>
                            <label type="button" class="btn btn-dark">Registros</label>
                        </a>
                    }

                    @if (cuenta.Permisos.Contains("TrabajadoresE"))
                    {
                        <a href='@Url.Action("Edit", "Trabajador", new { id = item.Id })'>
                            <label title="Editar" type="submit" class="btn btn-warning">Editar</label>
                        </a>
                    }
                    <a href='@Url.Action("Details", "Trabajador", new { id = item.Id })'>
                        <label title="Detalle" type="submit" class="btn btn-info">Detalle </label>
                    </a>
                    @if (cuenta.Permisos.Contains("TrabajadoresD"))
                    {
                        <a href='@Url.Action("Delete", "Trabajador", new { id = item.Id })' onclick="return confirma('¿Está seguro que desea eliminar el Trabajador?')"})>
                            <label title="Eliminar" type="submit" class="btn btn-danger">Borrar1</label>
                        </a>
                   }

                    @if (cuenta.Permisos.Contains("Sueldo"))
                    {
                        <a href='@Url.Action("Index", "Remuneracion", new { id = item.Id })'>
                            <label title="Remuneración" type="submit" class="btn btn-light">Remuneracion</label>
                        </a>
                    }

                    @if (cuenta.Permisos.Contains("TrabajadoresS"))
                    {
                        <a href='@Url.Action("PDFDetalle", "Trabajador", new { id = item.Id })'>
                            <label title="Solicitud Contratación" type="submit" class="btn btn-success">
                                Contrato
                            </label>
                        </a>
                    }
                    @if (cuenta.Permisos.Contains("TrabajadoresM"))
                    {
                        <a href='@Url.Action("Index", "Trabajador", new { id = item.Id })' target="_blank">

                            <label title="Correo" type="submit" class="btn btn-light">
                                Correo
                            </label>
                        </a>
                    }
                </td>

                </tr>
            }
    </table>
</div>

<script src="~/Scripts/Busqueda.js"></script>
