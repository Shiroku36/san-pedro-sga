@model IEnumerable<ControlPersonalAppWeb.Models.TrabajadorIndex>

@{
    ViewBag.Title = "Índice";
}
<h1 class="display-4 text-center">Índice</h1>
<hr />
    @Html.ActionLink("Crear nuevo", "Create",null, new { @class = "btn btn-primary mb-3 " })
<div class="table-responsive">
    <table class="table table-hover" id="tabla">
        <tr class="bg-primary">
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApellidoPaterno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApellidoMaterno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rut)
            </th>
            <th>Accion</th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ApellidoPaterno)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ApellidoMaterno)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Rut)
                </td>
                <td>
                    <label>
                        @Html.ActionLink("Editar", "Edit", new { id = item.Id }, new { @class = "btn btn-block btn-warning " })
                    </label>
                    <label>
                        @Html.ActionLink("Detalles", "Details", new { id = item.Id }, new { @class = "btn btn-block btn-info " })
                    </label>
                    <label>
                        <a href='@Url.Action("Delete", "Productos", new { id = item.Id })' onclick="return confirm('¿Está seguro que desea eliminar el trabajador?')" })>
                            <label title="Eliminar" type="submit" class="btn btn-danger mb-1">Borrar</label>
                        </a>
                    </label>

                </td>
            </tr>
        }

    </table>
</div>