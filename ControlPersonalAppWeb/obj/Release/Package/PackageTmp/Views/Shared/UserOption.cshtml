
@{
    ViewBag.Title = "UserOption";
    var cuenta = ControlPersonalAppWeb.Utils.SessionManager.CuentaAutenticada();
}

@if (cuenta != null)
{
    if (cuenta != null && cuenta.Nivel == 6)
    {
        <li class="mt-2"><a id="link-registro" onclick="to('/Registro/Index/'+@cuenta.TrabajadorId)">Registros</a></li>
    }
    <li class="mt-2"><a  href="@Url.Action("Details","Cuenta", new { cuenta.Id })">@cuenta.Usuario - @cuenta.Empresa</a></li>
    <li class="mt-2"><a href="@Url.Action("Salir","Cuenta")">Salir</a></li>
}
else
{
    <li><a href="@Url.Action("Login","Cuenta")">Ingresar</a></li>
}
<script>
    var x = window.matchMedia("(max-width: 576px)")
    var not = document.getElementById('notificationD');
    var btn = document.getElementById('sidebarCollapse');
    var badge = document.getElementById('badgee');
    var clkbtn = 0;
    if (!x.matches && btn != null) {
        btn.onclick = function () {
            if (not.style.display == "block") {
                salida();
                return;
            }
            entrada();
            clkbtn = 1

        }
        window.addEventListener('click', function (e) {
            if ((!(not.contains(e.target)) && clkbtn != 1)) {
                salida();
            }
            clkbtn = 0
        });
        function entrada() {
            not.style.display = "block";
            not.classList.remove('scale-out-hor-right')
            not.classList.add('scale-in-hor-right')
        }
        function salida() {
            not.classList.remove('scale-in-hor-right')
            not.classList.add('scale-out-hor-right')
            setTimeout(function () { not.style.display = "none"; }, 500);
        }
    } else {
        if (btn != null) {
            //btn.className = "d-none";
            badge.className += " position-relative";
            btn.onclick = function () {
                location.href = location.origin + "/Notificaciones";
                
            }
        }

    }
    function to(str) {
        location.href = location.origin + str;

    }
</script>

